package com.weddingRental;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Main entry point for the Wedding Rental Spring Boot application
@SpringBootApplication
public class WeddingRentalApplication {

    public static void main(String[] args) {
        // This will run the Spring Boot application
        SpringApplication.run(WeddingRentalApplication.class, args);
    }
}
